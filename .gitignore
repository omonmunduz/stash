# Dependencies
node_modules/
.pnp
.pnp.js

# Testing
coverage/

# Next.js
.next/
out/
build/
dist/

# Environment variables
.env
.env*.local
.env.production

# Local scratch files that have held credentials. s.js contains a plaintext
# API key; it is referenced by no source file. Ignored so it cannot enter
# git history. The key itself should be rotated regardless.
s.js
scratch.js

# Supabase
# config.toml IS committed: it is the local dev configuration and every
# developer needs the same one. Only generated/runtime state is ignored.
.supabase/
supabase/.branches/
supabase/.temp/

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# TypeScript
*.tsbuildinfo
next-env.d.ts
